const BACKEND_URL = 'http://localhost:8080';

function load_movie_data(){
    const movies_selection_field = document.getElementById('movie-selection');
    movies_selection_field.innerText = "movies appear here";
}
